'use strict';

//Validate license number plate.
exports.validateLicensePlate = function(req) {
    const licensePlate = req.params.licensePlate;
    //console.log("licensePlate:aaa " + licensePlate);

    return true;
}